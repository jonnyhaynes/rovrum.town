{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "cleanUrls": true,
  "//": "Serve the static holding page in public/ directly. No install/build — this",
  "// ": "is a pnpm+Turborepo monorepo with Prisma + a heavy Playwright/Chromium dep;",
  "//  ": "Vercel's zero-config path would install/build the workspace and fail. The",
  "//   ": "web app (Phase 2, Astro) will set its own build config when it lands.",
  "buildCommand": "",
  "installCommand": "",
  "outputDirectory": "public",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
      ]
    }
  ]
}
